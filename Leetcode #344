class Solution {
public:
    void reverseString(vector<char>& s) {
        int j = s.size()-1;
        int i = 0;
        while(i < j){
            char tmp = s[i];
            s[i] = s[j];
            s[j] = tmp;
            i++;
            j--;
        }
    }
};
