#include <bits/stdc++.h>
using namespace std;

int main()
{
    char s[100000];
    gets(s);
    int c=0,i=0;
    for(i=0;s[i]!='\0';i++)
    {
        if(s[i]=='r'&&s[i+1]=='r')
        c++;
    }
    cout<<c;
}

